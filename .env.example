# Sample Pack Alchemist — optional environment variables.
#
# The app is fully static: it runs without any configuration. The one env var
# below is optional and only affects the local dev server.

# Local dev server port (scripts/serve.mjs). Overrides `--port=<n>`.
SPA_PORT=8080
